version: 0.0
Resources:
  - TargetService:
      Type: AWS::EKS::Service
      Properties:
        TaskDefinition: k8s/product-deployment.yaml
        LoadBalancerInfo:
          ContainerName: product-service
          ContainerPort: 3000
